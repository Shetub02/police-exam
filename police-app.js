'use strict';
const D=window.POLICE_DATA,C=window.PoliceCore,app=document.querySelector('#app'),byId=new Map(D.questions.map(q=>[q.id,q]));
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const subjectName=id=>D.subjects.find(s=>s.id===id)?.name||id;
let storageOK=true;
let progressOwner=null,applyingProgress=false;
function progressKey(k){return progressOwner?'police-user:'+progressOwner+':'+k:k;}
function read(k,f){try{return JSON.parse(localStorage.getItem(progressKey(k)))??f;}catch{return f;}}
function write(k,v){try{localStorage.setItem(progressKey(k),JSON.stringify(v));}catch{storageOK=false;}if(!applyingProgress)window.dispatchEvent(new Event('police-progress-change'));}
let history=read('police-history-v4',[]);if(!Array.isArray(history))history=[];
let completedAttempts=read('police-completed-attempts-v1',[]);if(!Array.isArray(completedAttempts))completedAttempts=[];
const completedQuestionIds=()=>new Set(completedAttempts.flatMap(x=>Array.isArray(x.ids)?x.ids:[]));
let session=read('police-session-v4',null);
if(session&&(!Array.isArray(session.ids)||!session.ids.length||new Set(session.ids).size!==session.ids.length||session.ids.some(id=>!byId.has(id))||typeof session.answers!=='object'||!session.answers||!Array.isArray(session.flags)||!Number.isFinite(session.remaining)||!Number.isFinite(session.index)||session.index<0||session.index>=session.ids.length||!Number.isFinite(session.deadline)))session=null;
let settings={subject:'',kind:'all',count:150,provider:'priority',repeat:'unseen'},page='home';
const badge=q=>`<span class="badge ${q.representation==='mock'?'':'recall'}">${q.representation==='mock'?(q.provider==='guru-police'?'แนวข้อสอบ · Guru Police':'แนวข้อสอบ'):'เรียบเรียงจากความจำ · '+q.year}</span>`;
function save(){write('police-session-v4',session);}
function pool(){return D.questions.filter(q=>(settings.provider!=='guru'||q.provider==='guru-police')&&(!settings.subject||q.subject===settings.subject)&&(settings.kind==='all'||(settings.kind==='mock'?q.representation==='mock':q.representation!=='mock')));}
function home(){page='home';app.innerHTML=`<section class="hero"><div class="eyebrow">PRACTICE / PREPARE / PROGRESS</div><h1>ฝึกให้พร้อม ก่อนวันสอบจริง</h1><p class="muted">นายสิบตำรวจ สายป้องกันปราบปราม · เลือกชุดฝึก แล้วเริ่มทำได้เลย</p></section>${session&&!session.finished?'<div class="card topline"><div><b>มีชุดที่ทำค้างไว้</b><p class="muted">คำตอบและลำดับข้อเดิมยังอยู่</p></div><button data-action="resume" class="primary">ทำต่อจากเดิม</button></div>':''}<div class="grid"><section class="card"><h2>จัดชุดข้อสอบของคุณ</h2><div class="fields"><div><label for="subject">รูปแบบการฝึก</label><select id="subject"><option value="">สุ่มรวมทุกวิชา ตามสัดส่วน</option>${D.subjects.map(s=>`<option value="${s.id}">${s.name}</option>`).join('')}</select></div><div><label for="kind">คลังข้อสอบ / ปี</label><select id="kind"><option value="all">สุ่มรวมทุกแหล่ง + แนวข้อสอบ</option><option value="mock">แนวข้อสอบเท่านั้น</option><option value="recall">ปี 2566 · เรียบเรียงจากความจำ</option></select></div><div><label for="count">จำนวนข้อ</label><select id="count">${[1,5,10,20,30,60,150].map(n=>`<option value="${n}">${n} ข้อ</option>`).join('')}</select></div><div><label for="provider">แหล่งข้อสอบที่ต้องการเน้น</label><select id="provider"><option value="priority">เน้น Guru Police + เติมให้ครบสัดส่วน</option><option value="all">สุ่มทุกแหล่งเท่าเทียมกัน</option><option value="guru">เฉพาะ Guru Police (เลือกฝึกแยกวิชา)</option></select></div><div><label>เวลาฝึก</label><p id="duration"></p></div></div><p id="availability" class="muted"></p><div id="error" role="status" class="error"></div><div class="actions"><button class="primary" data-action="start">เริ่มชุดสุ่มใหม่ →</button></div><p class="muted"><small>ทุกข้อมีเฉลยและคำอธิบายหลังส่งคำตอบ · แนวข้อสอบมีป้ายกำกับทุกข้อ</small></p></section><aside class="card"><h2>สัดส่วนในชุดนี้</h2><div id="quotas"></div><p><small>ใช้สัดส่วนที่ตั้งไว้ในโปรเจกต์ ยังไม่ยืนยันประกาศรอบล่าสุด <a href="evidence.html#blueprint">ดูรายละเอียด</a></small></p></aside></div><section class="card"><h2>การฝึกล่าสุด</h2>${history.length?history.slice(0,8).map(h=>`<div class="quota"><span>${esc(h.date)} · ${esc(h.count)} ข้อ</span><span>คะแนน ${esc(h.correct)}/${esc(h.total)}</span></div>`).join(''):'<p class="muted">เริ่มชุดแรก แล้วกลับมาดูความก้าวหน้าที่นี่</p>'}${session?.finished?'<div class="actions"><button data-action="results">ดูเฉลยชุดล่าสุด</button></div>':''}</section>${!storageOK?'<p class="error">เบราว์เซอร์ไม่อนุญาตให้บันทึก กรุณาทำชุดนี้ให้จบก่อนปิดหน้า</p>':''}`;
for(const k of ['subject','kind','count','provider']){let el=document.getElementById(k);el.value=settings[k];el.onchange=()=>{settings[k]=k==='count'?Number(el.value):el.value;preview();};}preview();}
function preview(){let p=pool();document.querySelector('#duration').textContent=Math.ceil(settings.count*1.2)+' นาที';document.querySelector('#availability').textContent=`มีข้อสอบให้เลือก ${p.length} ข้อ · Guru Police ${p.filter(q=>q.provider==='guru-police').length} ข้อ · สุ่มไม่ซ้ำภายในชุด`;document.querySelector('#quotas').innerHTML=(settings.subject?[{id:settings.subject,n:settings.count}]:C.quotas(D.subjects,settings.count)).map(s=>`<div class="quota"><span>${esc(subjectName(s.id))}</span><b>${s.n} ข้อ</b></div>`).join('');document.querySelector('#error').textContent='';}
function remaining(){return session.paused?session.remaining:Math.max(0,session.deadline-Date.now());}
function start(){try{const qs=C.sample(pool(),D.subjects,settings.count,settings.subject,settings.provider==='priority'?'guru-police':null);if(session&&!session.finished&&!confirm('เริ่มชุดใหม่และแทนที่ชุดที่ยังทำไม่จบหรือไม่?'))return;let duration=Math.ceil(settings.count*1.2)*60000;session={ids:qs.map(q=>q.id),answers:{},flags:[],index:0,remaining:duration,deadline:Date.now()+duration,paused:false,finished:false,created:Date.now()};save();quiz();}catch(e){document.querySelector('#error').textContent=e.message;}}
function quiz(){page='quiz';if(session.finished)return results();let q=byId.get(session.ids[session.index]);app.innerHTML=`<div class="topline"><button data-action="home">← หน้าหลัก</button><span class="timer" id="timer" aria-label="เวลาที่เหลือ"></span><button data-action="pause">${session.paused?'จับเวลาต่อ':'พักเวลา'}</button></div><div class="grid"><section class="card"><div class="topline"><span>${esc(subjectName(q.subject))} · ข้อ ${session.index+1}/${session.ids.length}</span>${badge(q)}</div>${session.paused?'<div class="notice">พักการทำข้อสอบอยู่ กดจับเวลาต่อเพื่อแสดงโจทย์</div>':`<div class="question">${esc(q.question_text)}</div>${q.choices?Object.entries(q.choices).map(([key,value])=>`<button class="choice ${session.answers[q.id]===key?'selected':''}" data-answer="${key}" aria-pressed="${session.answers[q.id]===key}"><b>${key}</b><span>${esc(value)}</span></button>`).join(''):`<label for="response">คำตอบของคุณ</label><textarea id="response" placeholder="พิมพ์คำตอบ แล้วเทียบวิธีทำหลังส่งข้อสอบ">${esc(session.answers[q.id]||'')}</textarea><p><small>ต้นทางไม่มีตัวเลือกครบ ข้อนี้ให้เทียบเฉลยด้วยตนเอง</small></p>`}<div class="actions"><button data-action="flag">${session.flags.includes(q.id)?'★ ปักหมุดแล้ว':'☆ ปักหมุดทบทวน'}</button></div>`}<div class="actions"><button data-action="prev" ${session.index===0?'disabled':''}>← ก่อนหน้า</button><button data-action="next" ${session.index===session.ids.length-1?'disabled':''}>ข้อต่อไป →</button></div></section><aside class="card"><h2>แผงคำตอบ</h2><p class="muted">สีเขียว = ตอบแล้ว · ขอบทอง = ปักหมุด</p><div class="numbers">${session.ids.map((id,i)=>`<button aria-label="ไปข้อ ${i+1}" class="${session.answers[id]?'done ':''}${session.index===i?'current ':''}${session.flags.includes(id)?'flag':''}" data-index="${i}">${i+1}</button>`).join('')}</div><div class="actions"><button class="primary" data-action="finish">ส่งคำตอบและดูเฉลย</button></div><p id="save-status" class="muted">${storageOK?'บันทึกคำตอบอัตโนมัติในเครื่องนี้':'บันทึกไม่ได้ กรุณาอย่าปิดหน้า'}</p></aside></div>`;const field=document.querySelector('#response');if(field)field.oninput=()=>{session.answers[q.id]=field.value;save();};tick();}
function finish(auto=false){if(!session||session.finished)return;let missing=session.ids.filter(id=>!session.answers[id]).length;if(!auto&&!confirm(`ส่งคำตอบชุดนี้หรือไม่?${missing?' ยังไม่ได้ตอบ '+missing+' ข้อ':''}`))return;session.remaining=remaining();session.finished=true;session.paused=true;const score=C.score(session.ids.map(id=>byId.get(id)),session.answers);history.unshift({date:new Date().toLocaleString('th-TH'),count:session.ids.length,...score});history=history.slice(0,30);write('police-history-v4',history);save();results();}
function results(){page='results';const qs=session.ids.map(id=>byId.get(id)),s=C.score(qs,session.answers);app.innerHTML=`<div class="hero"><div class="eyebrow">YOUR PRACTICE REVIEW</div><h1>ทำเสร็จแล้ว มาทบทวนกัน</h1><button data-action="home">← กลับไปเลือกชุดฝึก</button></div><section class="card"><div class="stat">${s.correct} / ${s.total}</div><p>คะแนนตามเฉลยของชุดฝึก</p><p class="muted">ข้อเรียบเรียงจากความจำใช้ตัวเลือกที่สร้างเพิ่มเพื่อการฝึก · คะแนนนี้ไม่ใช่เฉลยทางการ</p><label for="review-filter">เลือกข้อที่ต้องการทบทวน</label><select id="review-filter"><option value="all">ทุกข้อ พร้อมเฉลย</option><option value="wrong">ข้อที่ตอบผิด / ยังไม่ตอบ</option><option value="flag">ข้อที่ปักหมุด</option></select></section><section id="reviews" class="card"></section>`;document.querySelector('#review-filter').onchange=review;review();}
function review(){let filter=document.querySelector('#review-filter').value;let qs=session.ids.map((id,i)=>({q:byId.get(id),i})).filter(({q})=>filter==='all'||(filter==='flag'?session.flags.includes(q.id):q.correct_answer&&session.answers[q.id]!==q.correct_answer));document.querySelector('#reviews').innerHTML=qs.length?qs.map(({q,i})=>{const answer=session.answers[q.id],wrong=q.correct_answer&&answer&&answer!==q.correct_answer,correct=q.correct_answer&&answer===q.correct_answer;return `<article class="resultrow ${wrong?'result-wrong':correct?'result-correct':''}">${badge(q)}<h3>ข้อ ${i+1} · ${esc(subjectName(q.subject))}${wrong?' <span class="wrong-mark">✕ ตอบผิด</span>':correct?' <span class="correct-mark">✓ ตอบถูก</span>':''}</h3>${q.reading_passage?`<section class="reading-passage" aria-label="บทอ่าน"><b>บทอ่าน</b><p>${esc(q.reading_passage)}</p></section>`:''}<div class="question">${esc(q.question_text)}</div>${q.choices?`<div class="review-choices">${Object.entries(q.choices).map(([k,v])=>{const state=k===answer&&k!==q.correct_answer?'wrong-choice':k===q.correct_answer?'correct-choice':'';const mark=state==='wrong-choice'?' <span>✕ คำตอบที่เลือก</span>':state==='correct-choice'?' <span>✓ คำตอบที่ถูก</span>':'';return `<p class="${state}"><b>${k}.</b> ${esc(v)}${mark}</p>`;}).join('')}</div>`:''}<p class="answer-status ${wrong?'wrong-text':correct?'correct-text':''}">คำตอบคุณ: <b>${esc(answer||'ยังไม่ได้ตอบ')}</b> ${q.correct_answer?(correct?'✓ ถูกตามเฉลย':answer?'✕ ไม่ตรงเฉลย':'· ยังไม่ได้ตอบ'):''}</p><div class="explain"><b>เฉลย: ${esc(q.source_answer)}</b><br>${q.explanation_th.map(esc).join('<br>')}</div><p><small>${q.explanation_author==='system'?'คำอธิบายเรียบเรียงเพิ่มเติมจากเงื่อนไขโจทย์':'คำอธิบายจากเอกสารแนวข้อสอบ'}${q.note?' · '+esc(q.note):''}</small></p><a href="evidence.html#${encodeURIComponent(q.id)}" target="_blank" rel="noopener">ดูหลักฐานของข้อนี้ ↗</a></article>`;}).join(''):'<p class="muted">ไม่มีข้อในตัวเลือกนี้</p>';}
function showPassage(){if(page!=='quiz'||!session||session.paused)return;let q=byId.get(session.ids[session.index]);if(!q?.reading_passage||document.querySelector('.reading-passage'))return;let target=document.querySelector('.question');if(!target)return;let box=document.createElement('section');box.className='reading-passage';box.setAttribute('aria-label','บทอ่าน');box.innerHTML='<b>บทอ่าน</b><p></p>';box.querySelector('p').textContent=q.reading_passage;target.before(box);}
function tick(){if(!session||session.finished)return;showPassage();let ms=remaining();if(ms<=0&&!session.paused){finish(true);return;}let el=document.querySelector('#timer');if(el){let t=Math.ceil(ms/1000);el.textContent=`${Math.floor(t/60).toString().padStart(2,'0')}:${(t%60).toString().padStart(2,'0')}${session.paused?' · พัก':''}`;}}
app.addEventListener('click',e=>{let b=e.target.closest('button');if(!b)return;const action=b.dataset.action;if(b.dataset.answer&&!session.paused){session.answers[session.ids[session.index]]=b.dataset.answer;save();quiz();return;}if(b.dataset.index!==undefined){session.index=Number(b.dataset.index);save();quiz();return;}if(action==='home')home();if(action==='start')start();if(action==='resume')quiz();if(action==='results')results();if(action==='finish')finish();if(action==='pause'){session.remaining=remaining();session.paused=!session.paused;session.deadline=Date.now()+session.remaining;save();quiz();}if(action==='prev'||action==='next'){session.index+=action==='next'?1:-1;save();quiz();}if(action==='flag'){const id=session.ids[session.index];session.flags=session.flags.includes(id)?session.flags.filter(x=>x!==id):[...session.flags,id];save();quiz();}});
function evidence(){page='evidence';const statuses={reviewed:'ตรวจภาพแล้ว · จากผู้รวบรวม',practice:'เอกสารแนวข้อสอบ',inaccessible:'ยังอ่านเนื้อหาไม่ได้',quarantined:'พักไว้ · ที่มาไม่ชัด',excluded:'ไม่ใช้เป็นข้อสอบจริง',pending:'รอตรวจรายข้อ',metadata:'ข้อมูลประกอบ'};const sourceLink=id=>{const s=D.sources.find(s=>s.id===id);return s?`<a href="#source-${esc(s.id)}">${esc(s.name)}</a>`:esc(id);};app.innerHTML=`<div class="hero"><div class="eyebrow">EVIDENCE / TRANSPARENCY</div><h1>หลักฐานและแหล่งอ้างอิง</h1><p class="muted">ตรวจและคัดข้อมูลเมื่อ ${esc(D.checked_at)} · เปิดเผยส่วนที่ยังยืนยันไม่ได้</p></div><section class="card"><h2>อ่านสถานะข้อสอบให้ตรงกัน</h2><p><span class="badge">แนวข้อสอบ</span> ${D.questions.filter(q=>q.representation==='mock').length} ข้อ จากเอกสารแนวข้อสอบและข้อที่เรียบเรียงจากแหล่งติว มีที่มารายข้อและคำอธิบาย ไม่ใช่การรับรองคำตอบทางการหรือกฎหมายฉบับล่าสุด</p><p><span class="badge recall">เรียบเรียงจากความจำ</span> 20 ข้อ ปี 2566 ตรวจเทียบภาพที่ผู้รวบรวมเผยแพร่ ตัวเลือกเดิมไม่ครบจึงสร้างตัวเลือก 4 ข้อเพิ่มเพื่อการฝึกและตรวจคะแนน โดยไม่อ้างว่าเป็นตัวเลือกจากสนามสอบ</p><p>ยังไม่มีข้อที่ยืนยันทั้งต้นฉบับกระดาษสอบและเฉลยทางการครบทุกส่วน ไม่ใช้ชื่อปีบนหนังสือแนวข้อสอบเป็นปีที่ข้อสอบออกจริง</p></section><section class="card" id="blueprint"><h2>สัดส่วนสำหรับชุดสุ่ม</h2>${D.subjects.map(s=>`<div class="quota"><span>${esc(s.name)}</span><b>${s.quota} / 150</b></div>`).join('')}<p class="notice">${esc(D.blueprint.note)} · ตั้งเวลา 180 นาทีสำหรับ 150 ข้อ ชุดสั้นกระจายตามอัตราส่วนและปัดเศษให้รวมเท่าจำนวนที่เลือก</p></section><section class="card"><h2>รายการที่พักไว้หลังตรวจ</h2>${D.excluded_items.map(x=>`<p><b>${esc(x.locator)}</b> — ${esc(x.reason)} (${sourceLink(x.source_id)})</p>`).join('')}<p class="muted">ไฟล์ PDF ที่ตรวจรายการ: ${D.localInventory.length} ไฟล์ไม่ซ้ำ · มีไฟล์สแกนและข้อความที่ยังต้องตรวจเพิ่ม การมีชื่ออยู่ในรายการไม่ได้หมายความว่าตรวจคำตอบทุกข้อในไฟล์แล้ว</p></section><section class="card"><h2>แหล่งข้อมูล ${D.sources.length} รายการ</h2>${D.sources.map(s=>`<article class="source resultrow" id="source-${esc(s.id)}"><span class="badge">${esc(statuses[s.status]||s.status)}</span><h3>${esc(s.name)}</h3><p>${esc(s.note)}</p><a href="${esc(s.local?encodeURI(s.url):s.url)}" target="_blank" rel="noopener">เปิด${s.local?'ไฟล์ต้นทางในเครื่อง':'แหล่งต้นทาง'} ↗</a></article>`).join('')}</section><section class="card"><h2>หลักฐานรายข้อ</h2>${D.questions.map(q=>`<article class="source resultrow" id="${esc(q.id)}">${badge(q)}<h3>${esc(subjectName(q.subject))} · ${esc(q.source_locator)}</h3><p>${esc(q.question_text)}</p><p>โจทย์: ${sourceLink(q.source_id)}${q.explanation_source_id?'<br>เฉลย: '+sourceLink(q.explanation_source_id):''}</p><p><small>${esc(q.verification_scope||(q.choices_generated?'ตรวจภาพโจทย์และคำตอบของผู้รวบรวม · ตัวเลือกสร้างเพิ่มเพื่อการฝึก ไม่ใช่ตัวเลือกจากสนามสอบ':'ตรวจภาพโจทย์และคำตอบของผู้รวบรวม ไม่ใช่ต้นฉบับทางการ'))}</small></p></article>`).join('')}</section><details class="card"><summary>รายการไฟล์ในเครื่อง</summary>${D.localInventory.map(f=>`<p>${esc(f.path)} · ${f.pages} หน้า · ${esc(f.status)}</p>`).join('')}</details>`;if(location.hash)requestAnimationFrame(()=>document.getElementById(decodeURIComponent(location.hash.slice(1)))?.scrollIntoView());}

function guruEvidence(){
 const sources=D.sources.filter(s=>s.provider==='guru-police');
 const labels={practice:'คัดข้อไว้ฝึกแล้ว',metadata:'ช่องทางและเอกสารประกอบ',pending:'พบแล้ว · ยังตรวจรายข้อไม่ครบ',inaccessible:'เปิดเนื้อหาไม่ได้'};
 const card=document.createElement('section');card.className='card';card.id='guru-police';
 card.innerHTML='<h2>พี่บัส Guru Police · แหล่งที่เน้นค้น</h2><p>คัดเรียบเรียง '+D.questions.filter(q=>q.provider==='guru-police').length+' ข้อจากเอกสารและบทถอดเสียง YouTube พร้อมเฉลย คลังรวม '+D.questions.length+' ข้อ</p><p>บนหน้า Home เลือกเน้น Guru Police เพื่อสุ่มข้อที่มีในแต่ละวิชาก่อน แล้วเติมจากคลังให้ครบสัดส่วน หรือเลือกเฉพาะ Guru Police และฝึกแยกวิชาได้</p><p class="muted">Facebook เปิดแล้วแจ้งว่าเนื้อหาไม่พร้อมใช้งาน ส่วน Instagram ยังไม่พบหลักฐานเชื่อมโยงบัญชีที่แน่ชัด และ TikTok พบเพียงรายการจากเว็บอื่น จึงยังไม่อ้างว่าได้ตรวจข้อสอบจากแพลตฟอร์มเหล่านั้น</p>'+
 sources.map(s=>'<article class="quota source"><div><span class="badge">'+esc(s.platform)+'</span><h3><a href="'+esc(s.url)+'" target="_blank" rel="noopener">'+esc(s.name)+' ↗</a></h3><p>'+esc(s.note)+'</p><small>'+esc(labels[s.status]||s.status)+'</small></div></article>').join('')+
 '<details><summary>หลักเกณฑ์การคัดข้อ</summary><p>ตัดข้อที่ต้องใช้ภาพประกอบ และข้อที่ยึดเกณฑ์อายุหรือกฎหมายเดิมซึ่งอาจเปลี่ยนแปลงแล้ว ทุกข้อที่นำเข้าได้รับการเรียบเรียงใหม่เป็น 4 ตัวเลือกพร้อมเฉลย</p></details>';
 document.querySelector('.hero').after(card);
}

const baseQuiz=quiz;
quiz=function(){baseQuiz();if(page!=='quiz'||!session||session.finished)return;const seen=completedQuestionIds(),legend=document.querySelector('.numbers')?.previousElementSibling;document.querySelectorAll('.numbers button[data-index]').forEach((button,i)=>{if(seen.has(session.ids[i])){button.classList.add('seen');button.setAttribute('aria-label',button.getAttribute('aria-label')+' · เคยทำแล้ว');button.insertAdjacentHTML('beforeend','<i aria-hidden="true"></i>');}});if(legend)legend.textContent='เขียว = ตอบในชุดนี้ · จุดม่วง = เคยทำในชุดที่ส่งแล้ว · ขอบทอง = ปักหมุด';};
const baseFinish=finish;
finish=function(auto=false){const before=session&&!session.finished;baseFinish(auto);if(!before||!session?.finished)return;const score=C.score(session.ids.map(id=>byId.get(id)),session.answers),date=new Date().toLocaleString('th-TH');completedAttempts.unshift({date,ids:[...session.ids],answers:{...session.answers},...score});completedAttempts=completedAttempts.slice(0,30);write('police-completed-attempts-v1',completedAttempts);};
const baseHome=home;
home=function(){baseHome();const logs=completedAttempts.slice(0,30);const card=document.createElement('section');card.className='card';card.innerHTML=`<h2>บันทึกชุดที่ทำเสร็จ</h2>${logs.length?`<p class="muted">เก็บไว้ ${logs.length} ชุดล่าสุด · ข้อที่อยู่ในบันทึกจะมีจุดม่วงในแผงคำตอบของชุดใหม่</p>${logs.map(log=>`<details class="attempt-log"><summary>${esc(log.date)} · ${log.ids.length} ข้อ · คะแนน ${log.correct}/${log.total}</summary><ol>${log.ids.map(id=>{const q=byId.get(id),answer=log.answers?.[id],state=!answer?'ยังไม่ได้ตอบ':answer===q?.correct_answer?'ตอบถูก':'ตอบผิด';return `<li><b>${esc(subjectName(q?.subject||''))}</b> · ${esc(state)}<br><span class="muted">${esc(q?.question_text||id)}</span></li>`;}).join('')}</ol></details>`).join('')}`:'<p class="muted">เมื่อส่งคำตอบ ชุดนี้จะถูกเก็บพร้อมรายชื่อข้อสอบและผลการตอบ</p>'}`;app.append(card);};
const progressStyle=document.createElement('style');progressStyle.textContent='.numbers button{position:relative}.numbers .seen{border-color:#8560a8;background:#f2eafd}.numbers .seen i{position:absolute;top:4px;right:4px;width:6px;height:6px;border-radius:50%;background:#7950a4}.numbers .seen.done{background:#dceee6}.attempt-log{border-top:1px solid #dde5eb;padding:13px 0}.attempt-log summary{cursor:pointer}.attempt-log ol{margin:12px 0 0;padding-left:24px}.attempt-log li{margin:10px 0;line-height:1.55}.attempt-log li .muted{font-size:14px}:root[data-theme="dark"] .numbers .seen{background:#312343;border-color:#ad8ad0}:root[data-theme="dark"] .numbers .seen i{background:#d0aaff}:root[data-theme="dark"] .numbers .seen.done{background:#194d49}:root[data-theme="dark"] .attempt-log{border-color:#31556d}';document.head.append(progressStyle);
settings.subjects=D.subjects.map(s=>s.id);
pool=function(){return D.questions.filter(q=>(settings.provider!=='guru'||q.provider==='guru-police')&&settings.subjects.includes(q.subject)&&(settings.kind==='all'||(settings.kind==='mock'?q.representation==='mock':q.representation!=='mock')));};
preview=function(){const selected=D.subjects.filter(s=>settings.subjects.includes(s.id)),p=pool();document.querySelector('#duration').textContent=Math.ceil(settings.count*1.2)+' นาที';document.querySelector('#availability').textContent=`มีข้อสอบให้เลือก ${p.length} ข้อ · Guru Police ${p.filter(q=>q.provider==='guru-police').length} ข้อ · สุ่มไม่ซ้ำภายในชุด`;document.querySelector('#quotas').innerHTML=selected.length?C.quotas(selected,settings.count).map(s=>`<div class="quota"><span>${esc(subjectName(s.id))}</span><b>${s.n} ข้อ</b></div>`).join(''):'<p class="error">กรุณาติ๊กเลือกอย่างน้อย 1 วิชา</p>';document.querySelector('#error').textContent='';};
start=function(){try{const selected=D.subjects.filter(s=>settings.subjects.includes(s.id));if(!selected.length)throw Error('กรุณาติ๊กเลือกอย่างน้อย 1 วิชา');const completed=completedQuestionIds();if(session?.ids)session.ids.forEach(id=>completed.add(id));const wrong=new Set(completedAttempts.flatMap(log=>(log.ids||[]).filter(id=>log.answers?.[id]!==byId.get(id)?.correct_answer)));const qs=C.sampleSmart(pool(),selected,settings.count,{mode:settings.repeat,preferredProvider:settings.provider==='priority'?'guru-police':null,completedIds:[...completed],wrongIds:[...wrong]});if(session&&!session.finished&&!confirm('เริ่มชุดใหม่และแทนที่ชุดที่ยังทำไม่จบหรือไม่?'))return;let duration=Math.ceil(settings.count*1.2)*60000;session={ids:qs.map(q=>q.id),answers:{},flags:[],index:0,remaining:duration,deadline:Date.now()+duration,paused:false,finished:false,created:Date.now()};save();quiz();}catch(e){document.querySelector('#error').textContent=e.message;}};
const homeWithProgressLog=home;
home=function(){homeWithProgressLog();const oldSelect=document.querySelector('#subject'),box=oldSelect?.closest('div');if(!box)return;box.innerHTML=`<fieldset class="subject-picker"><legend>เลือกวิชาที่จะออกในชุดนี้</legend><div>${D.subjects.map(s=>`<label><input type="checkbox" value="${s.id}" ${settings.subjects.includes(s.id)?'checked':''}> ${esc(s.name)}</label>`).join('')}</div></fieldset>`;box.querySelectorAll('input').forEach(input=>input.onchange=()=>{settings.subjects=[...box.querySelectorAll('input:checked')].map(x=>x.value);preview();});};
progressStyle.textContent+='.subject-picker{margin:0;padding:0;border:0}.subject-picker legend{margin-bottom:8px}.subject-picker>div{display:grid;grid-template-columns:1fr 1fr;gap:8px}.subject-picker label{display:flex;align-items:center;gap:7px;margin:0;font-weight:500}.subject-picker input{width:17px;height:17px;padding:0;accent-color:#153b56}:root[data-theme="dark"] .subject-picker input{accent-color:#f4c76c}@media(max-width:520px){.subject-picker>div{grid-template-columns:1fr}}';
const baseResults=results;
results=function(){baseResults();if(page!=='results'||!session)return;const rows=D.subjects.map(subject=>{const qs=session.ids.map(id=>byId.get(id)).filter(q=>q?.subject===subject.id&&q.correct_answer&&q.choices);if(!qs.length)return null;const correct=qs.filter(q=>session.answers[q.id]===q.correct_answer).length,pct=Math.round(correct*100/qs.length);return {name:subject.name,correct,total:qs.length,pct};}).filter(Boolean);const card=document.createElement('section');card.className='card subject-results';card.innerHTML=`<h2>ผลแยกตามวิชา</h2><p class="muted">ดูจุดที่ควรทบทวนจากเปอร์เซ็นต์ของแต่ละวิชา</p><div class="subject-rings">${rows.map(row=>`<div class="subject-ring-item"><div class="subject-ring" style="--pct:${row.pct}" role="img" aria-label="${esc(row.name)} ${row.pct} เปอร์เซ็นต์"><b>${row.pct}%</b></div><div><b>${esc(row.name)}</b><br><small>ตอบถูก ${row.correct}/${row.total} ข้อ</small></div></div>`).join('')}</div>`;document.querySelector('#reviews').before(card);};
progressStyle.textContent+='.subject-rings{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.subject-ring-item{display:flex;align-items:center;gap:12px;padding:10px;border:1px solid #dbe2e8;border-radius:10px}.subject-ring{width:64px;height:64px;border-radius:50%;display:grid;place-items:center;flex:0 0 64px;background:radial-gradient(circle,#fff 58%,transparent 59%),conic-gradient(#27719a calc(var(--pct)*1%),#dce6ed 0);font-size:14px}.subject-ring b{font-size:15px}:root[data-theme="dark"] .subject-ring-item{border-color:#31556d}:root[data-theme="dark"] .subject-ring{background:radial-gradient(circle,#10283a 58%,transparent 59%),conic-gradient(#5ca3d4 calc(var(--pct)*1%),#29485d 0)}@media(max-width:620px){.subject-rings{grid-template-columns:1fr}.subject-ring-item{padding:9px}}';
const resultsWithSubjectStats=results;
results=function(){resultsWithSubjectStats();if(page!=='results'||!session)return;const footer=document.createElement('section');footer.className='card results-footer';footer.innerHTML='<div class="actions"><button class="primary" data-action="home">← กลับหน้าหลัก</button></div>';document.querySelector('#reviews').after(footer);};
const homeWithPercentages=home;
home=function(){homeWithPercentages();const historyCard=[...app.querySelectorAll('.card')].find(card=>card.querySelector('h2')?.textContent==='การฝึกล่าสุด');if(!historyCard)return;historyCard.querySelectorAll('.quota').forEach((row,index)=>{const entry=history[index];if(!entry||!entry.total)return;const percent=Math.round(entry.correct*100/entry.total);row.lastElementChild.textContent=`ตอบถูก ${entry.correct}/${entry.total} (${percent}%)`;});};
const homeWithFreshRandom=home;
home=function(){homeWithFreshRandom();const provider=document.querySelector('#provider');if(!provider)return;const holder=document.createElement('div');holder.innerHTML='<label for="repeat">การเลือกข้อจากประวัติ</label><select id="repeat"><option value="unseen">เน้นข้อที่ยังไม่เคยทำ</option><option value="all">สุ่มจากทุกข้อ</option><option value="wrong">ทบทวนข้อที่เคยผิดก่อน</option></select>';provider.closest('div').after(holder);const repeat=holder.querySelector('select');repeat.value=settings.repeat;repeat.onchange=()=>{settings.repeat=repeat.value;preview();};preview();};
const previewWithFreshCounts=preview;
preview=function(){previewWithFreshCounts();const p=pool(),done=completedQuestionIds();if(session?.ids)session.ids.forEach(id=>done.add(id));const selected=D.subjects.filter(s=>settings.subjects.includes(s.id)),unique=new Set(p.map(C.questionKey)).size,unseen=p.filter(q=>!done.has(q.id)).length,needed=C.quotas(selected,settings.count).reduce((sum,s)=>sum+Math.max(0,s.n-p.filter(q=>q.subject===s.id&&!done.has(q.id)).length),0),mode=settings.repeat==='unseen'?'จะเลือกข้อที่ยังไม่เคยทำก่อน':settings.repeat==='wrong'?'จะเลือกข้อที่เคยตอบผิดก่อน':'สุ่มจากทุกข้อ';document.querySelector('#availability').textContent=`มี ${p.length} ข้อ (${unique} เนื้อหาไม่ซ้ำ) · ยังไม่เคยทำ ${unseen} ข้อ · ${mode}${settings.repeat==='unseen'&&needed?` · ตามสัดส่วนนี้จำเป็นต้องใช้ข้อเก่าอย่างน้อย ${needed} ข้อ`:''}`;};
const evidenceWithCutoffStats=evidence;
evidence=function(){evidenceWithCutoffStats();const stats=D.cutoff_stats||[];if(!stats.length)return;const card=document.createElement('section');card.className='card cutoff-stats';card.id='cutoff-stats';card.innerHTML=`<h2>คะแนนสอบย้อนหลังและข้อมูลบอกเล่า</h2><p class="muted">ใช้ดูแนวโน้มจากข้อมูลที่หาได้เท่านั้น คะแนนและจำนวนรับเปลี่ยนตามปี พื้นที่ และประกาศของแต่ละรอบ</p>${stats.map(stat=>{const source=D.sources.find(s=>s.id===stat.source_id);const sourceHtml=source?`<a href="${esc(source.url)}" target="_blank" rel="noopener">เปิดแหล่งข้อมูล ↗</a>`:'';const hasScore=stat.score!==null||stat.score_label;const label=stat.confidence==='rumor'?'ข่าวลือ / คำบอกเล่า':stat.confidence==='unavailable'?'ยังยืนยันคะแนนไม่ได้':'ข้อมูลย้อนหลังจากเว็บรวบรวม';return `<article class="cutoff-row"><div><span class="badge ${(stat.confidence==='unavailable'||stat.confidence==='rumor')?'recall':''}">${label}</span><h3>${esc(stat.unit)} · ปี ${esc(stat.year)}</h3><p>${esc(stat.scope)}${stat.range?' · ช่วงคะแนน '+esc(stat.range):''}</p><p class="muted">${esc(stat.note)}</p>${sourceHtml}</div>${hasScore?`<div class="cutoff-score ${stat.confidence==='rumor'?'cutoff-rumor':''}"><b>${esc(stat.score_label||(stat.score+'/'+stat.total))}</b>${stat.percent!==null?`<span>${esc(stat.percent)}%</span>`:''}<small>${stat.confidence==='rumor'?'ตามคำบอกเล่า':'ต่ำสุดที่รายงาน'}</small></div>`:`<div class="cutoff-unavailable"><b>ไม่พบคะแนนตัด</b><small>ที่ตรวจยืนยันได้</small></div>`}</article>`;}).join('')}`;document.querySelector('#blueprint')?.after(card);};
progressStyle.textContent+='.cutoff-row{display:flex;justify-content:space-between;gap:20px;padding:18px 0;border-top:1px solid #dbe2e8}.cutoff-row:first-of-type{border-top:0}.cutoff-row h3{margin:8px 0}.cutoff-row p{margin:7px 0}.cutoff-score,.cutoff-unavailable{min-width:150px;display:flex;flex-direction:column;align-items:flex-end;justify-content:center;text-align:right}.cutoff-score b{font-size:27px;color:#153b56}.cutoff-score span{font-size:20px;font-weight:700;color:#27719a}.cutoff-score small,.cutoff-unavailable small{margin-top:4px}.cutoff-rumor b{color:#8a4b00}.cutoff-unavailable b{color:#9b5a00}:root[data-theme="dark"] .cutoff-row{border-color:#31556d}:root[data-theme="dark"] .cutoff-score b{color:#e7f4ff}:root[data-theme="dark"] .cutoff-score span{color:#7fc5f1}:root[data-theme="dark"] .cutoff-rumor b,:root[data-theme="dark"] .cutoff-unavailable b{color:#f4c76c}@media(max-width:620px){.cutoff-row{flex-direction:column}.cutoff-score,.cutoff-unavailable{align-items:flex-start;text-align:left}}';
window.PoliceProgress={
  snapshot:()=>JSON.parse(JSON.stringify({version:1,history,attempts:completedAttempts,session})),
  active:()=>page==='quiz'&&session&&!session.finished,
  switchOwner(owner,state){progressOwner=owner;this.apply(state);},
  apply(state){
    const value=window.PoliceSyncCore.validate(state);
    if(value.session?.ids.some(id=>!byId.has(id)))throw Error('ชุดข้อสอบมีข้อใหม่ กรุณารีเฟรชเว็บก่อนซิงก์');
    history=value.history;completedAttempts=value.attempts;session=value.session;
    applyingProgress=true;
    try{write('police-history-v4',history);write('police-completed-attempts-v1',completedAttempts);save();}finally{applyingProgress=false;}
    if(!location.pathname.endsWith('evidence.html'))home();
  }
};
if(location.pathname.endsWith('evidence.html')){evidence();guruEvidence();}else{home();setInterval(tick,1000);}
